apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pruthvi-sample-app-route
  namespace: openshift-ztpspec:
  host: route-sophisticated-tarsier-openshift-ztp.apps.cbco.sandbox215.opentlc.com
  path: /
  to:
    kind: Service
    name: pruthvi-sample-app-service
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None
